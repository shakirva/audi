const { ChartOfAccount, JournalEntry, JournalEntryLine, sequelize } = require("../models");

async function run() {
  console.log("Updating getDashboard to use ledger instead of polyfill...");
}
run();
